
module.exports = (Photos, Reviews, TitleMap, Sidebar) => `
  <div id="TitleMap">${TitleMap}</div>
  <div id="Photos">${Photos}</div>
  <div id="Reviews">${Reviews}</div>
  <div id="Sidebar">${Sidebar}</div>
`;
